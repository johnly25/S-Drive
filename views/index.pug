extends layout 
block content
    .container-fluid.p-5
        .row
            .col.h-100 
                h1 S-Drive
                if user 
                    h1 Welcome #{user.username}
                else 
                    p login to use s-drive
                if error
                    h2 #{error}
                include test.pug